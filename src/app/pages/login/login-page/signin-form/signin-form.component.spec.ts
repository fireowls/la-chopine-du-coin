import { SigninFormComponent } from './signin-form.component';

describe('SigninPagesComponent', () => {
  let component: SigninFormComponent;

  beforeEach(() => {
    component = new SigninFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
